<%#= debug @start_date %>

<%#= debug(@measurements_by_minute) %>

<%
now = Date.today
start_date = (now - 1)
%>

<div id="samples_per_minute"></div>
<script>

series_by_minute = JSON.parse('<%= @series_by_minute.to_json.html_safe %>')

$(function () {
    $('#samples_per_minute').highcharts({
		title: {
		    text: 'Samples per Minute'
		},
        chart: {
			type: 'line',
            zoomType: 'x',
            spacing: [0,0,0,0]
        },
        xAxis: {
            type: 'datetime',
            tickInterval: 5*60000,
        },
        yAxis: {
        	min: 0,
            title: {
                text: 'Samples'
            }
        },
		legend: {
			title: 'test'
		},
		series: 
		  series_by_minute,
    });
});
</script>
