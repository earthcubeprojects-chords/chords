<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= content_for?(:title) ? yield(:title) : "CHORDS Portal" %></title>
    <meta name="description" content="<%= content_for?(:description) ? yield(:description) : "CHORDS Portal" %>">
    <%= favicon_link_tag 'CHORDS_7.png' %>
    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track' => true %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
    <%= csrf_meta_tags %>
	<script>

	$(document).ready(function() {
	  $('.best_in_place').best_in_place();
	})
	</script>

  </head>

  <div class="container" data-no-turbolink>
    <!-- top banner -->
    <%= render 'layouts/bannerbar' %>
    
    <!-- top navbar -->
    <%= render 'layouts/topnav' %>
    
      <!-- main area -->
      <div class="col-xs-12 col-sm-9">
        <%= render 'layouts/messages' %>
        <%= yield %>
      </div> <!-- /.col-xs-12 main -->
    </div>     <!--/.container-->
</html>
