<%= form_with(model: netcdf_datum, local: true) do |form| %>
  <% if netcdf_datum.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(netcdf_datum.errors.count, "error") %> prohibited this netcdf_datum from being saved:</h2>

      <ul>
      <% netcdf_datum.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
